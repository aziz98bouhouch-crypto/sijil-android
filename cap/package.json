{
  "name": "sijil-android",
  "version": "1.0.0",
  "private": true,
  "description": "غلاف أندرويد (Capacitor) لتطبيق المواكبة التربوية الذكية",
  "scripts": {
    "copy:web": "node copy-web.js",
    "build:android": "npm run copy:web && cap add android || true",
    "sync": "npm run copy:web && cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
